# Task Script Board

Static web app that renders a task script into a graph + kanban view.

## Run Locally
```bash
python serve.py
```
Then open `http://0.0.0.0:4000` in your browser.

## Project Structure
- `index.html` main entry.
- `scripts/` JavaScript modules.
- `styles/` CSS.
